[
  {
    "toolSlug": "Pinterest_GetUserAccount",
    "toolPurpose": "Retrieve authenticated user account information including profile, username, and account details.",
    "created_by": "human",
    "parameters": {
      "type": "object",
      "properties": {},
      "required": []
    },
    "inputsSpec": {
      "required": [],
      "optional": []
    }
  },
  {
    "toolSlug": "Pinterest_ListBoards",
    "toolPurpose": "List all boards owned by the authenticated user with pagination support.",
    "created_by": "human",
    "parameters": {
      "type": "object",
      "properties": {
        "bookmark": {
          "type": "string",
          "description": "Pagination cursor for fetching next set of boards"
        },
        "page_size": {
          "type": "integer",
          "description": "Number of boards to retrieve per page (default: 25, max: 250)",
          "minimum": 1,
          "maximum": 250
        },
        "fields": {
          "type": "array",
          "description": "Specific board fields to include in response (e.g., 'id', 'name', 'description', 'privacy')",
          "items": { "type": "string" }
        }
      },
      "required": []
    },
    "inputsSpec": {
      "required": [],
      "optional": ["bookmark", "page_size", "fields"]
    }
  },
  {
    "toolSlug": "Pinterest_ListPins",
    "toolPurpose": "List all pins on a specific board with pagination and field filtering.",
    "created_by": "human",
    "parameters": {
      "type": "object",
      "properties": {
        "board_id": {
          "type": "string",
          "description": "ID of the board to retrieve pins from"
        },
        "bookmark": {
          "type": "string",
          "description": "Pagination cursor for fetching next set of pins"
        },
        "page_size": {
          "type": "integer",
          "description": "Number of pins to retrieve per page (default: 25, max: 250)",
          "minimum": 1,
          "maximum": 250
        },
        "fields": {
          "type": "array",
          "description": "Specific pin fields to include (e.g., 'id', 'created_at', 'note', 'link', 'image_url')",
          "items": { "type": "string" }
        }
      },
      "required": ["board_id"]
    },
    "inputsSpec": {
      "required": ["board_id"],
      "optional": ["bookmark", "page_size", "fields"]
    }
  },
  {
    "toolSlug": "Pinterest_CreatePin",
    "toolPurpose": "Create a new pin on a specified board with title, description, media source, and optional external link.",
    "created_by": "human",
    "parameters": {
      "type": "object",
      "properties": {
        "board_id": {
          "type": "string",
          "description": "ID of the board to create the pin on"
        },
        "title": {
          "type": "string",
          "description": "Pin title (required for pin creation)"
        },
        "description": {
          "type": "string",
          "description": "Pin description or caption"
        },
        "link": {
          "type": "string",
          "description": "External URL the pin should link to (optional)"
        },
        "media_source": {
          "type": "object",
          "description": "Media source for the pin (URL or upload)",
          "properties": {
            "source_type": {
              "type": "string",
              "enum": ["image_url"],
              "description": "Type of media source"
            },
            "url": {
              "type": "string",
              "description": "URL of the image to pin (required for pin creation)"
            }
          },
          "required": ["source_type", "url"]
        }
      },
      "required": ["board_id", "title", "media_source"]
    },
    "inputsSpec": {
      "required": ["board_id", "title", "media_source"],
      "optional": ["description", "link"]
    }
  },
  {
    "toolSlug": "Pinterest_SearchPins",
    "toolPurpose": "Search for pins across Pinterest by query term with pagination and optional sorting.",
    "created_by": "human",
    "parameters": {
      "type": "object",
      "properties": {
        "query": {
          "type": "string",
          "description": "Search query term (e.g., 'interior design', 'home decor')"
        },
        "bookmark": {
          "type": "string",
          "description": "Pagination cursor for fetching next set of search results"
        },
        "page_size": {
          "type": "integer",
          "description": "Number of results to retrieve per page (default: 25, max: 250)",
          "minimum": 1,
          "maximum": 250
        },
        "fields": {
          "type": "array",
          "description": "Specific pin fields to include in results",
          "items": { "type": "string" }
        }
      },
      "required": ["query"]
    },
    "inputsSpec": {
      "required": ["query"],
      "optional": ["bookmark", "page_size", "fields"]
    }
  }
]
