[
  {
    "toolSlug": "OpenSea_GetCollection",
    "toolPurpose": "Retrieve detailed metadata for a specific NFT collection by slug.",
    "created_by": "human",
    "httpMethod": "GET",
    "endpoint": "/collections/{collection_slug}",
    "parameters": {
      "type": "object",
      "properties": {
        "collection_slug": {
          "type": "string",
          "description": "Collection slug (e.g., 'boredapeyachtclub', 'cryptopunks')"
        }
      },
      "required": [
        "collection_slug"
      ]
    },
    "inputsSpec": {
      "required": [
        "collection_slug"
      ],
      "optional": []
    }
  },
  {
    "toolSlug": "OpenSea_ListNFTs",
    "toolPurpose": "List NFTs within a collection with pagination and optional trait filtering.",
    "created_by": "human",
    "httpMethod": "GET",
    "endpoint": "/collection/{collection_slug}/nfts",
    "parameters": {
      "type": "object",
      "properties": {
        "collection_slug": {
          "type": "string",
          "description": "Collection slug"
        },
        "limit": {
          "type": "integer",
          "description": "Results per page (max 200)",
          "maximum": 200,
          "default": 50
        },
        "next": {
          "type": "string",
          "description": "Pagination cursor"
        }
      },
      "required": [
        "collection_slug"
      ]
    },
    "inputsSpec": {
      "required": [
        "collection_slug"
      ],
      "optional": [
        "limit",
        "next"
      ]
    }
  },
  {
    "toolSlug": "OpenSea_GetNFT",
    "toolPurpose": "Retrieve detailed data for a specific NFT by chain, contract address, and token ID.",
    "created_by": "human",
    "httpMethod": "GET",
    "endpoint": "/chain/{chain}/contract/{address}/nfts/{identifier}",
    "parameters": {
      "type": "object",
      "properties": {
        "chain": {
          "type": "string",
          "description": "Blockchain (ethereum, polygon, solana, etc.)"
        },
        "address": {
          "type": "string",
          "description": "Contract address"
        },
        "identifier": {
          "type": "string",
          "description": "Token ID"
        }
      },
      "required": [
        "chain",
        "address",
        "identifier"
      ]
    },
    "inputsSpec": {
      "required": [
        "chain",
        "address",
        "identifier"
      ],
      "optional": []
    }
  },
  {
    "toolSlug": "OpenSea_GetCollectionStats",
    "toolPurpose": "Get collection statistics including floor price, total volume, total supply, and number of owners.",
    "created_by": "human",
    "httpMethod": "GET",
    "endpoint": "/collections/{collection_slug}/stats",
    "parameters": {
      "type": "object",
      "properties": {
        "collection_slug": {
          "type": "string",
          "description": "Collection slug"
        }
      },
      "required": [
        "collection_slug"
      ]
    },
    "inputsSpec": {
      "required": [
        "collection_slug"
      ],
      "optional": []
    }
  },
  {
    "toolSlug": "OpenSea_ListEvents",
    "toolPurpose": "List recent events (sales, transfers, listings) for a collection or specific NFT.",
    "created_by": "human",
    "httpMethod": "GET",
    "endpoint": "/events/collection/{collection_slug}",
    "parameters": {
      "type": "object",
      "properties": {
        "collection_slug": {
          "type": "string",
          "description": "Collection slug"
        },
        "event_type": {
          "type": "array",
          "description": "Event types to filter",
          "items": {
            "type": "string",
            "enum": [
              "sale",
              "transfer",
              "listing",
              "offer",
              "cancel"
            ]
          }
        },
        "after": {
          "type": "string",
          "description": "Pagination cursor"
        },
        "limit": {
          "type": "integer",
          "description": "Results per page",
          "maximum": 50,
          "default": 20
        }
      },
      "required": [
        "collection_slug"
      ]
    },
    "inputsSpec": {
      "required": [
        "collection_slug"
      ],
      "optional": [
        "event_type",
        "after",
        "limit"
      ]
    }
  },
  {
    "toolSlug": "OpenSea_GetListings",
    "toolPurpose": "Get active listings for a collection sorted by price.",
    "created_by": "human",
    "httpMethod": "GET",
    "endpoint": "/listings/collection/{collection_slug}/all",
    "parameters": {
      "type": "object",
      "properties": {
        "collection_slug": {
          "type": "string",
          "description": "Collection slug"
        },
        "limit": {
          "type": "integer",
          "description": "Results per page (max 100)",
          "maximum": 100,
          "default": 20
        },
        "next": {
          "type": "string",
          "description": "Pagination cursor"
        }
      },
      "required": [
        "collection_slug"
      ]
    },
    "inputsSpec": {
      "required": [
        "collection_slug"
      ],
      "optional": [
        "limit",
        "next"
      ]
    }
  }
]