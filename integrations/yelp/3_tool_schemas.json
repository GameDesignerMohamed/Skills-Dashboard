[
  {
    "toolSlug": "Yelp_SearchBusinesses",
    "toolPurpose": "Search for businesses by term, location, category, and sort criteria. Returns paginated list of matching businesses with ratings, reviews count, and contact information.",
    "created_by": "human",
    "parameters": {
      "type": "object",
      "properties": {
        "term": {
          "type": "string",
          "description": "Search term for business type or name (e.g., 'restaurants', 'coffee shops', 'plumbers')"
        },
        "location": {
          "type": "string",
          "description": "Location for search (e.g., 'San Francisco, CA', 'New York', postal code)"
        },
        "categories": {
          "type": "array",
          "description": "Array of Yelp category aliases to filter by (e.g., ['restaurants', 'chinese', 'pizza'])",
          "items": { "type": "string" }
        },
        "sort_by": {
          "type": "string",
          "description": "Sort order for results",
          "enum": ["best_match", "rating", "review_count", "distance"]
        },
        "limit": {
          "type": "integer",
          "description": "Number of results to return (1-50, default: 20)",
          "minimum": 1,
          "maximum": 50
        },
        "offset": {
          "type": "integer",
          "description": "Offset for pagination (use with limit)",
          "minimum": 0
        },
        "price": {
          "type": "array",
          "description": "Price filter (1=cheap, 2=moderate, 3=expensive, 4=very expensive)",
          "items": { "type": "integer", "minimum": 1, "maximum": 4 }
        },
        "rating": {
          "type": "number",
          "description": "Minimum rating filter (1.0-5.0)",
          "minimum": 1.0,
          "maximum": 5.0
        },
        "open_now": {
          "type": "boolean",
          "description": "Filter to only open businesses now"
        }
      },
      "required": ["term", "location"]
    },
    "inputsSpec": {
      "required": ["term", "location"],
      "optional": ["categories", "sort_by", "limit", "offset", "price", "rating", "open_now"]
    }
  },
  {
    "toolSlug": "Yelp_GetBusiness",
    "toolPurpose": "Retrieve detailed information for a specific business including hours, photos, attributes, price range, and address.",
    "created_by": "human",
    "parameters": {
      "type": "object",
      "properties": {
        "business_id": {
          "type": "string",
          "description": "Unique Yelp business ID (obtained from search results)"
        }
      },
      "required": ["business_id"]
    },
    "inputsSpec": {
      "required": ["business_id"],
      "optional": []
    }
  },
  {
    "toolSlug": "Yelp_GetReviews",
    "toolPurpose": "Retrieve customer reviews for a specific business, sorted by newest or most relevant, with review text and ratings.",
    "created_by": "human",
    "parameters": {
      "type": "object",
      "properties": {
        "business_id": {
          "type": "string",
          "description": "Unique Yelp business ID"
        },
        "limit": {
          "type": "integer",
          "description": "Number of reviews to return (1-50, default: 20)",
          "minimum": 1,
          "maximum": 50
        },
        "sort": {
          "type": "string",
          "description": "Sort order for reviews",
          "enum": ["newest", "useful", "cool", "funny"]
        }
      },
      "required": ["business_id"]
    },
    "inputsSpec": {
      "required": ["business_id"],
      "optional": ["limit", "sort"]
    }
  },
  {
    "toolSlug": "Yelp_SearchByPhone",
    "toolPurpose": "Find a specific business by its phone number, returning business details and ID for further queries.",
    "created_by": "human",
    "parameters": {
      "type": "object",
      "properties": {
        "phone": {
          "type": "string",
          "description": "Business phone number in E.164 format (e.g., '+14155552671' or '(415) 555-2671')"
        }
      },
      "required": ["phone"]
    },
    "inputsSpec": {
      "required": ["phone"],
      "optional": []
    }
  },
  {
    "toolSlug": "Yelp_Autocomplete",
    "toolPurpose": "Get autocomplete suggestions for business names and categories based on partial text input, with optional geographic scope.",
    "created_by": "human",
    "parameters": {
      "type": "object",
      "properties": {
        "text": {
          "type": "string",
          "description": "Partial text to autocomplete (e.g., 'cof' for coffee)"
        },
        "latitude": {
          "type": "number",
          "description": "Latitude for location-aware suggestions"
        },
        "longitude": {
          "type": "number",
          "description": "Longitude for location-aware suggestions"
        },
        "locale": {
          "type": "string",
          "description": "Locale for results (e.g., 'en_US')"
        }
      },
      "required": ["text"]
    },
    "inputsSpec": {
      "required": ["text"],
      "optional": ["latitude", "longitude", "locale"]
    }
  }
]
