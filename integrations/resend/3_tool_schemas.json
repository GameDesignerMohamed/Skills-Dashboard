[
  {
    "toolSlug": "Resend_SendEmail",
    "toolPurpose": "Send a transactional email with HTML or plain text content to one or more recipients.",
    "created_by": "human",
    "httpMethod": "POST",
    "endpoint": "/emails",
    "parameters": {
      "type": "object",
      "properties": {
        "from": {
          "type": "string",
          "description": "Sender email address (must be from verified domain)"
        },
        "to": {
          "type": "array",
          "description": "Recipient email addresses",
          "items": {
            "type": "string"
          }
        },
        "subject": {
          "type": "string",
          "description": "Email subject line"
        },
        "html": {
          "type": "string",
          "description": "HTML email body"
        },
        "text": {
          "type": "string",
          "description": "Plain text email body (fallback)"
        },
        "cc": {
          "type": "array",
          "description": "CC recipients",
          "items": {
            "type": "string"
          }
        },
        "bcc": {
          "type": "array",
          "description": "BCC recipients",
          "items": {
            "type": "string"
          }
        },
        "reply_to": {
          "type": "string",
          "description": "Reply-to email address"
        }
      },
      "required": [
        "from",
        "to",
        "subject"
      ]
    },
    "inputsSpec": {
      "required": [
        "from",
        "to",
        "subject"
      ],
      "optional": [
        "html",
        "text",
        "cc",
        "bcc",
        "reply_to"
      ]
    }
  },
  {
    "toolSlug": "Resend_GetEmail",
    "toolPurpose": "Retrieve details and delivery status for a previously sent email by ID.",
    "created_by": "human",
    "httpMethod": "GET",
    "endpoint": "/emails/{email_id}",
    "parameters": {
      "type": "object",
      "properties": {
        "email_id": {
          "type": "string",
          "description": "Email ID returned from send operation"
        }
      },
      "required": [
        "email_id"
      ]
    },
    "inputsSpec": {
      "required": [
        "email_id"
      ],
      "optional": []
    }
  },
  {
    "toolSlug": "Resend_ListEmails",
    "toolPurpose": "List recently sent emails with delivery status and metadata.",
    "created_by": "human",
    "httpMethod": "GET",
    "endpoint": "/emails",
    "parameters": {
      "type": "object",
      "properties": {},
      "required": []
    },
    "inputsSpec": {
      "required": [],
      "optional": []
    }
  },
  {
    "toolSlug": "Resend_ListDomains",
    "toolPurpose": "List all verified sending domains configured in the Resend account.",
    "created_by": "human",
    "httpMethod": "GET",
    "endpoint": "/domains",
    "parameters": {
      "type": "object",
      "properties": {},
      "required": []
    },
    "inputsSpec": {
      "required": [],
      "optional": []
    }
  },
  {
    "toolSlug": "Resend_GetDomain",
    "toolPurpose": "Get detailed information about a specific sending domain including DNS records and verification status.",
    "created_by": "human",
    "httpMethod": "GET",
    "endpoint": "/domains/{domain_id}",
    "parameters": {
      "type": "object",
      "properties": {
        "domain_id": {
          "type": "string",
          "description": "Domain ID"
        }
      },
      "required": [
        "domain_id"
      ]
    },
    "inputsSpec": {
      "required": [
        "domain_id"
      ],
      "optional": []
    }
  },
  {
    "toolSlug": "Resend_ListApiKeys",
    "toolPurpose": "List API keys associated with the Resend account (names and IDs only, not secrets).",
    "created_by": "human",
    "httpMethod": "GET",
    "endpoint": "/api-keys",
    "parameters": {
      "type": "object",
      "properties": {},
      "required": []
    },
    "inputsSpec": {
      "required": [],
      "optional": []
    }
  }
]