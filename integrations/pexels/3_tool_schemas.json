[
  {
    "toolSlug": "Pexels_SearchPhotos",
    "toolPurpose": "Search the Pexels photo library by query term with pagination support. Returns multiple photo sizes and photographer attribution details.",
    "created_by": "human",
    "parameters": {
      "type": "object",
      "properties": {
        "query": {
          "type": "string",
          "description": "Search query term (e.g., 'nature', 'technology', 'people', 'sunset')"
        },
        "per_page": {
          "type": "integer",
          "description": "Number of photos per page (1-80, default: 15)",
          "minimum": 1,
          "maximum": 80
        },
        "page": {
          "type": "integer",
          "description": "Page number for pagination (default: 1)",
          "minimum": 1
        },
        "orientation": {
          "type": "string",
          "description": "Filter by orientation",
          "enum": ["portrait", "landscape", "square"]
        },
        "size": {
          "type": "string",
          "description": "Filter by photo size",
          "enum": ["large", "medium", "small"]
        },
        "locale": {
          "type": "string",
          "description": "Locale code for search results (e.g., 'en-US', 'es-ES', 'fr-FR')"
        }
      },
      "required": ["query"]
    },
    "inputsSpec": {
      "required": ["query"],
      "optional": ["per_page", "page", "orientation", "size", "locale"]
    }
  },
  {
    "toolSlug": "Pexels_GetPhoto",
    "toolPurpose": "Retrieve detailed information about a specific photo by ID, including all available sizes and photographer details for attribution.",
    "created_by": "human",
    "parameters": {
      "type": "object",
      "properties": {
        "photo_id": {
          "type": "string",
          "description": "The unique identifier of the photo to retrieve"
        }
      },
      "required": ["photo_id"]
    },
    "inputsSpec": {
      "required": ["photo_id"],
      "optional": []
    }
  },
  {
    "toolSlug": "Pexels_CuratedPhotos",
    "toolPurpose": "Retrieve curated and featured photos selected by the Pexels editorial team. Provides a rotating selection of high-quality images.",
    "created_by": "human",
    "parameters": {
      "type": "object",
      "properties": {
        "per_page": {
          "type": "integer",
          "description": "Number of photos per page (1-80, default: 15)",
          "minimum": 1,
          "maximum": 80
        },
        "page": {
          "type": "integer",
          "description": "Page number for pagination (default: 1)",
          "minimum": 1
        }
      },
      "required": []
    },
    "inputsSpec": {
      "required": [],
      "optional": ["per_page", "page"]
    }
  },
  {
    "toolSlug": "Pexels_SearchVideos",
    "toolPurpose": "Search the Pexels video library by query term. Returns videos with multiple quality options and videographer attribution information.",
    "created_by": "human",
    "parameters": {
      "type": "object",
      "properties": {
        "query": {
          "type": "string",
          "description": "Search query term for videos (e.g., 'landscape', 'abstract', 'nature', 'technology')"
        },
        "per_page": {
          "type": "integer",
          "description": "Number of videos per page (1-80, default: 15)",
          "minimum": 1,
          "maximum": 80
        },
        "page": {
          "type": "integer",
          "description": "Page number for pagination (default: 1)",
          "minimum": 1
        },
        "orientation": {
          "type": "string",
          "description": "Filter by video orientation",
          "enum": ["portrait", "landscape", "square"]
        }
      },
      "required": ["query"]
    },
    "inputsSpec": {
      "required": ["query"],
      "optional": ["per_page", "page", "orientation"]
    }
  },
  {
    "toolSlug": "Pexels_GetFeaturedCollections",
    "toolPurpose": "Retrieve featured photo collections curated by Pexels. Collections group related photos into thematic sets.",
    "created_by": "human",
    "parameters": {
      "type": "object",
      "properties": {
        "per_page": {
          "type": "integer",
          "description": "Number of collections per page (1-80, default: 15)",
          "minimum": 1,
          "maximum": 80
        },
        "page": {
          "type": "integer",
          "description": "Page number for pagination (default: 1)",
          "minimum": 1
        }
      },
      "required": []
    },
    "inputsSpec": {
      "required": [],
      "optional": ["per_page", "page"]
    }
  }
]
