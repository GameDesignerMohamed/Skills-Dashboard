[
  {
    "toolSlug": "Unsplash_SearchPhotos",
    "toolPurpose": "Search Unsplash photo library by keyword with optional filters for orientation, color, size, and content category.",
    "created_by": "human",
    "parameters": {
      "type": "object",
      "properties": {
        "query": {
          "type": "string",
          "description": "Search keyword or phrase (e.g., 'mountain', 'urban landscape', 'nature')"
        },
        "page": {
          "type": "integer",
          "description": "Pagination page number (default: 1)",
          "minimum": 1
        },
        "per_page": {
          "type": "integer",
          "description": "Number of results per page (default: 10, max: 30)",
          "minimum": 1,
          "maximum": 30
        },
        "orientation": {
          "type": "string",
          "description": "Filter by image orientation",
          "enum": ["landscape", "portrait", "squarish"]
        },
        "color": {
          "type": "string",
          "description": "Filter by dominant color",
          "enum": ["black_and_white", "black", "white", "yellow", "orange", "red", "purple", "magenta", "green", "teal", "blue"]
        },
        "order_by": {
          "type": "string",
          "description": "Sort results by relevance or popularity",
          "enum": ["relevant", "latest"]
        },
        "content_filter": {
          "type": "string",
          "description": "Filter by content safety",
          "enum": ["low", "high"]
        }
      },
      "required": ["query"]
    },
    "inputsSpec": {
      "required": ["query"],
      "optional": ["page", "per_page", "orientation", "color", "order_by", "content_filter"]
    }
  },
  {
    "toolSlug": "Unsplash_GetPhoto",
    "toolPurpose": "Retrieve detailed metadata for a single photo including ID, URL, dimensions, photographer info, and download statistics.",
    "created_by": "human",
    "parameters": {
      "type": "object",
      "properties": {
        "photo_id": {
          "type": "string",
          "description": "Unique Unsplash photo ID"
        },
        "w": {
          "type": "integer",
          "description": "Optional custom width for image URL (respects aspect ratio)"
        },
        "h": {
          "type": "integer",
          "description": "Optional custom height for image URL (respects aspect ratio)"
        }
      },
      "required": ["photo_id"]
    },
    "inputsSpec": {
      "required": ["photo_id"],
      "optional": ["w", "h"]
    }
  },
  {
    "toolSlug": "Unsplash_GetRandomPhoto",
    "toolPurpose": "Retrieve one or more random photos with optional filtering by query, topics, collections, featured status, username, or count.",
    "created_by": "human",
    "parameters": {
      "type": "object",
      "properties": {
        "count": {
          "type": "integer",
          "description": "Number of random photos to return (default: 1, max: 30)",
          "minimum": 1,
          "maximum": 30
        },
        "query": {
          "type": "string",
          "description": "Optional keyword to filter random results (e.g., 'nature', 'urban')"
        },
        "topics": {
          "type": "array",
          "description": "Array of topic IDs to filter by curated topics",
          "items": { "type": "string" }
        },
        "collections": {
          "type": "array",
          "description": "Array of collection IDs to filter by collections",
          "items": { "type": "string" }
        },
        "featured": {
          "type": "boolean",
          "description": "Limit results to featured photos only"
        },
        "username": {
          "type": "string",
          "description": "Limit results to photos by a specific photographer"
        }
      },
      "required": []
    },
    "inputsSpec": {
      "required": [],
      "optional": ["count", "query", "topics", "collections", "featured", "username"]
    }
  },
  {
    "toolSlug": "Unsplash_ListTopics",
    "toolPurpose": "List all curated photo topics available on Unsplash with metadata including topic ID, slug, and description.",
    "created_by": "human",
    "parameters": {
      "type": "object",
      "properties": {
        "page": {
          "type": "integer",
          "description": "Pagination page number (default: 1)",
          "minimum": 1
        },
        "per_page": {
          "type": "integer",
          "description": "Number of topics per page (default: 20, max: 30)",
          "minimum": 1,
          "maximum": 30
        },
        "order_by": {
          "type": "string",
          "description": "Sort results by featured status or position",
          "enum": ["featured", "latest", "oldest"]
        }
      },
      "required": []
    },
    "inputsSpec": {
      "required": [],
      "optional": ["page", "per_page", "order_by"]
    }
  },
  {
    "toolSlug": "Unsplash_GetCollectionPhotos",
    "toolPurpose": "Retrieve all photos from a specific Unsplash collection with pagination and optional ordering.",
    "created_by": "human",
    "parameters": {
      "type": "object",
      "properties": {
        "collection_id": {
          "type": "string",
          "description": "Unique Unsplash collection ID"
        },
        "page": {
          "type": "integer",
          "description": "Pagination page number (default: 1)",
          "minimum": 1
        },
        "per_page": {
          "type": "integer",
          "description": "Number of photos per page (default: 10, max: 30)",
          "minimum": 1,
          "maximum": 30
        },
        "order_by": {
          "type": "string",
          "description": "Sort results by addition date or relevance",
          "enum": ["latest", "oldest", "popular"]
        }
      },
      "required": ["collection_id"]
    },
    "inputsSpec": {
      "required": ["collection_id"],
      "optional": ["page", "per_page", "order_by"]
    }
  }
]
